import { ThemeProviderProps } from '../theme-provider';

/**
 * @description will change to interface if more combination of provider happens in the future
 */
export type JuloProviderProps = ThemeProviderProps;
