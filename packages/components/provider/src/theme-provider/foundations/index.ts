import colors from './colors';
import fontSizes from './font-sizes';
import lineHeights from './line-heights';

const DEFAULT_THEME = { colors, fontSizes, lineHeights };

export { DEFAULT_THEME, colors, fontSizes, lineHeights };
