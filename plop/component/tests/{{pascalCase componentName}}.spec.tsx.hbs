import { render, screen } from '@julo-ui/rtl-utils';

import {{pascalCase componentName}} from '../src';

describe('{{pascalCase componentName}}', () => {
  test('should render {{pascalCase componentName}}', () => {
    render(<{{pascalCase componentName}} />);
    screen.getByText('Hello World');
  });
});
