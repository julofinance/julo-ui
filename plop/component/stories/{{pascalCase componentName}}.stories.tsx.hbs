import { Story, Meta } from '@storybook/react';

import {{pascalCase componentName}}, { {{pascalCase componentName}}Props } from '../src';

export default {
  title: 'Components/{{pascalCase componentName}}',
  component: {{pascalCase componentName}},
  parameters: {
    docs: {
      description: {
        component:
          "`import { {{pascalCase componentName}} } from '@julo-ui/{{componentName}}';`",
      },
    },
  },
} as Meta<typeof {{pascalCase componentName}}>;

const Template: Story<{{pascalCase componentName}}Props > = (args) => (
  <{{pascalCase componentName}} {...args} />
);

export const {{pascalCase componentName}}Test = Template.bind({});